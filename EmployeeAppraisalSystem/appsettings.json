{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  // Configure the database connection string
  // The "ConnectionStrings" section provides the connection string for the database.
  // - "DefaultConnection" is the named connection string.
  // - "Server" specifies the SQL Server instance, "DESKTOP-BQS4MS8\\SQLEXPRESS" in this case.
  // - "Database" specifies the name of the database, which is "AppraisalManagement" here.
  // - "Trusted_Connection=True" means Windows integrated authentication is used.
  // - "TrustServerCertificate=True" specifies that the application trusts the server certificate.
  "ConnectionStrings": {
    "DefaultConnection": "Server=DESKTOP-BQS4MS8\\SQLEXPRESS;Database=AppraisalManagement;Trusted_Connection=True;TrustServerCertificate=True"
  }
}
